var React = require('react');

var Foo = React.createClass({
    render: function () {
        return (
        	<div>
        		<strong>ReactJS here!</strong>
        	</div>
        );
    }
});

module.exports = Foo;
